# Core
fastapi>=0.115.0
uvicorn[standard]>=0.34.0
pydantic>=2.10.0
email-validator>=2.1.0
pydantic-settings>=2.7.0
python-dotenv>=1.0.0
PyJWT>=2.8.0
# Bắt buộc cho UploadFile/Form ở /editor/upload — thiếu là FastAPI crash lúc import
python-multipart>=0.0.9

# AI / LangChain
langchain>=0.3.0
langchain-core>=0.3.0
langchain-openai>=0.3.0
langchain-groq>=0.3.0
langchain-community>=0.3.0
langgraph>=0.2.0

# Database
sqlalchemy>=2.0.0
alembic>=1.14.0
asyncpg>=0.29.0
psycopg2-binary>=2.9.0
aiosqlite>=0.20.0
greenlet>=3.0.0

# ---- Tầng dữ liệu / RAG (src/rag) -----------------------------------------
# Những gói cần để CHẠY app (truy xuất từ vector store). Phần nặng dùng để
# ingest nằm riêng ở requirements-rag.txt, xem ghi chú ở file đó.
chromadb>=1.5.0           # vector store, chạy local, persist ra data/vectorstore
cohere>=5.13.0            # embedding embed-multilingual-v3.0 — MẶC ĐỊNH
openai>=1.60.0            # embedding qua API, phương án thay thế
tiktoken>=0.8.0           # đếm token khi cắt chunk
PyYAML>=6.0               # đọc data/registry.yaml
#
# sentence-transformers CHỈ cần khi RAG_EMBEDDING_PROVIDER=local (bge-m3). Nó
# kéo theo torch nên nằm ở requirements-rag.txt, không cài mặc định. Mặc định
# dùng Cohere: nhanh hơn khoảng 30 lần khi ingest, và server không phải nạp
# model 2.5GB RAM nên deploy được lên free tier.

# Dev tools
ruff>=0.8.0
types-PyYAML>=6.0        # stub cho mypy, không có thì `mypy src/` báo lỗi ở registry.py
pytest>=8.0.0
pytest-asyncio>=0.24.0
httpx>=0.28.0

# Evaluation
ragas>=0.1.0
datasets>=2.16.0
