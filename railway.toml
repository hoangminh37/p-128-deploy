[build]
builder = "NIXPACKS"

[deploy]
startCommand = "python scripts/init_db.py && uvicorn src.main:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/api/v1/health"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
